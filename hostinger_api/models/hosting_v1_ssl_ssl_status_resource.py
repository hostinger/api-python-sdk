# coding: utf-8

"""
    Hostinger API Python SDK

    API Version: 1.54.2

    NOTE: This file is auto-generated, DO NOT EDIT THIS FILE MANUALLY!
    If you want to contribute or request a new feature, please create an issue or pull request on https://github.com/hostinger/api
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class HostingV1SslSslStatusResource(BaseModel):
    """
    HostingV1SslSslStatusResource
    """ # noqa: E501
    status: StrictStr = Field(description="Current certificate status")
    provider: Optional[StrictStr] = Field(description="Provider of the assigned certificate, or of the last recorded installation when none is assigned. `custom` means an uploaded certificate. Null when no certificate is assigned and no installation is recorded, which is also the case for free subdomains on the platform-managed certificate.")
    is_lifetime: StrictBool = Field(description="Whether the certificate comes from a lifetime provider managed by the platform, not an uploaded one. Follows `provider`: it reflects the last recorded installation when no certificate is assigned, and is false when `provider` is null.")
    is_https_redirect_enabled: StrictBool = Field(description="Whether HTTP requests to the website are redirected to HTTPS")
    expires_at: Optional[datetime] = Field(description="End of the assigned certificate validity period; null when no certificate details are available.")
    last_error: Optional[StrictStr] = Field(description="Last installation error, when it is one of the known displayable messages")
    __properties: ClassVar[List[str]] = ["status", "provider", "is_lifetime", "is_https_redirect_enabled", "expires_at", "last_error"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['not_installed', 'installing', 'waiting_for_retry', 'active', 'expired', 'failed']):
            raise ValueError("must be one of enum values ('not_installed', 'installing', 'waiting_for_retry', 'active', 'expired', 'failed')")
        return value

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['custom', 'letsencrypt', 'zerossl', 'googlessl', 'hssl']):
            raise ValueError("must be one of enum values ('custom', 'letsencrypt', 'zerossl', 'googlessl', 'hssl')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HostingV1SslSslStatusResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if provider (nullable) is None
        # and model_fields_set contains the field
        if self.provider is None and "provider" in self.model_fields_set:
            _dict['provider'] = None

        # set to None if expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.expires_at is None and "expires_at" in self.model_fields_set:
            _dict['expires_at'] = None

        # set to None if last_error (nullable) is None
        # and model_fields_set contains the field
        if self.last_error is None and "last_error" in self.model_fields_set:
            _dict['last_error'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HostingV1SslSslStatusResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "provider": obj.get("provider"),
            "is_lifetime": obj.get("is_lifetime"),
            "is_https_redirect_enabled": obj.get("is_https_redirect_enabled"),
            "expires_at": obj.get("expires_at"),
            "last_error": obj.get("last_error")
        })
        return _obj


