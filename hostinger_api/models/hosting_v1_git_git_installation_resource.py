# coding: utf-8

"""
    Hostinger API Python SDK

    API Version: 1.54.0

    NOTE: This file is auto-generated, DO NOT EDIT THIS FILE MANUALLY!
    If you want to contribute or request a new feature, please create an issue or pull request on https://github.com/hostinger/api
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class HostingV1GitGitInstallationResource(BaseModel):
    """
    HostingV1GitGitInstallationResource
    """ # noqa: E501
    uuid: StrictStr = Field(description="Installation identifier. Use it as the path parameter of List Git installation repositories.")
    provider: StrictStr = Field(description="Git provider the account belongs to")
    account_login: Optional[StrictStr] = Field(description="Login of the connected provider account (user or organization)")
    account_type: Optional[StrictStr] = Field(description="Whether the connected account is a user or an organization")
    account_avatar_url: Optional[StrictStr] = Field(description="Avatar URL of the connected provider account")
    status: StrictStr = Field(description="Installation status. Only active installations are listed unless the status filter says otherwise.")
    installed_at: Optional[datetime] = Field(description="When the provider app was installed on the account")
    created_at: datetime = Field(description="When the installation record was created")
    has_oauth: Optional[StrictBool] = Field(description="True when the GitHub user account has a stored OAuth token whose refresh token is still valid, false when the token is missing or its refresh token expired. Null for organization accounts and for providers other than GitHub.")
    __properties: ClassVar[List[str]] = ["uuid", "provider", "account_login", "account_type", "account_avatar_url", "status", "installed_at", "created_at", "has_oauth"]

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['github', 'gitlab', 'bitbucket']):
            raise ValueError("must be one of enum values ('github', 'gitlab', 'bitbucket')")
        return value

    @field_validator('account_type')
    def account_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['User', 'Organization']):
            raise ValueError("must be one of enum values ('User', 'Organization')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending', 'active', 'suspended']):
            raise ValueError("must be one of enum values ('pending', 'active', 'suspended')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HostingV1GitGitInstallationResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if account_login (nullable) is None
        # and model_fields_set contains the field
        if self.account_login is None and "account_login" in self.model_fields_set:
            _dict['account_login'] = None

        # set to None if account_type (nullable) is None
        # and model_fields_set contains the field
        if self.account_type is None and "account_type" in self.model_fields_set:
            _dict['account_type'] = None

        # set to None if account_avatar_url (nullable) is None
        # and model_fields_set contains the field
        if self.account_avatar_url is None and "account_avatar_url" in self.model_fields_set:
            _dict['account_avatar_url'] = None

        # set to None if installed_at (nullable) is None
        # and model_fields_set contains the field
        if self.installed_at is None and "installed_at" in self.model_fields_set:
            _dict['installed_at'] = None

        # set to None if has_oauth (nullable) is None
        # and model_fields_set contains the field
        if self.has_oauth is None and "has_oauth" in self.model_fields_set:
            _dict['has_oauth'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HostingV1GitGitInstallationResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "uuid": obj.get("uuid"),
            "provider": obj.get("provider"),
            "account_login": obj.get("account_login"),
            "account_type": obj.get("account_type"),
            "account_avatar_url": obj.get("account_avatar_url"),
            "status": obj.get("status"),
            "installed_at": obj.get("installed_at"),
            "created_at": obj.get("created_at"),
            "has_oauth": obj.get("has_oauth")
        })
        return _obj


