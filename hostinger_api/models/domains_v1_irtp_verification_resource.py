# coding: utf-8

"""
    Hostinger API Python SDK

    API Version: 1.46.1

    NOTE: This file is auto-generated, DO NOT EDIT THIS FILE MANUALLY!
    If you want to contribute or request a new feature, please create an issue or pull request on https://github.com/hostinger/api
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class DomainsV1IRTPVerificationResource(BaseModel):
    """
    DomainsV1IRTPVerificationResource
    """ # noqa: E501
    domain: Optional[StrictStr] = Field(default=None, description="Domain name")
    status: Optional[StrictStr] = Field(default=None, description="IRTP verification status")
    old_confirmed_at: Optional[datetime] = Field(default=None, description="When the old registrant confirmed the change")
    new_confirmed_at: Optional[datetime] = Field(default=None, description="When the new registrant confirmed the change")
    old_whois_profile_email: Optional[StrictStr] = Field(default=None, description="Email the old registrant confirmation was sent to")
    new_whois_profile_email: Optional[StrictStr] = Field(default=None, description="Email the new registrant confirmation was sent to")
    expires_at: Optional[datetime] = Field(default=None, description="When the verification auto-cancels if unconfirmed")
    __properties: ClassVar[List[str]] = ["domain", "status", "old_confirmed_at", "new_confirmed_at", "old_whois_profile_email", "new_whois_profile_email", "expires_at"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'completed', 'canceled']):
            raise ValueError("must be one of enum values ('pending', 'completed', 'canceled')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DomainsV1IRTPVerificationResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if old_confirmed_at (nullable) is None
        # and model_fields_set contains the field
        if self.old_confirmed_at is None and "old_confirmed_at" in self.model_fields_set:
            _dict['old_confirmed_at'] = None

        # set to None if new_confirmed_at (nullable) is None
        # and model_fields_set contains the field
        if self.new_confirmed_at is None and "new_confirmed_at" in self.model_fields_set:
            _dict['new_confirmed_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DomainsV1IRTPVerificationResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "domain": obj.get("domain"),
            "status": obj.get("status"),
            "old_confirmed_at": obj.get("old_confirmed_at"),
            "new_confirmed_at": obj.get("new_confirmed_at"),
            "old_whois_profile_email": obj.get("old_whois_profile_email"),
            "new_whois_profile_email": obj.get("new_whois_profile_email"),
            "expires_at": obj.get("expires_at")
        })
        return _obj


